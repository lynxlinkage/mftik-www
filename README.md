# mftik.com

Static product page for [mftik.com](https://mftik.com). Instances live on `*.mftik.app`.

No backend. Open `index.html` or any static server:

```bash
python3 -m http.server 5174
```

GitHub Pages: this repo’s `CNAME` is `mftik.com`. DNS is still on Hostinger until you point it here.
